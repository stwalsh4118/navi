# [1-3] Create project file structure

[Back to task list](./tasks.md)

## Description

Set up the file structure as specified in the PRD, creating placeholder files for all components.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-02-05 00:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2026-02-05 01:33:51 | Status Change | Proposed | InProgress | Started implementation | AI_Agent |
| 2026-02-05 01:34:17 | Status Change | InProgress | Review | Implementation complete, ready for review | AI_Agent |

## Requirements

Create the following structure:
```
claude-sessions/
├── main.go              # entry point, Bubble Tea program setup
├── model.go             # Model, Init, Update, View
├── sessions.go          # polling logic, JSON parsing, stale cleanup
├── styles.go            # Lip Gloss style definitions
├── hooks/
│   └── notify.sh        # hook script for Claude Code to write status
├── install.sh           # copies hooks, patches claude settings.json
├── go.mod
├── go.sum
└── README.md
```

## Implementation Plan

1. Create `hooks/` directory
2. Create placeholder `main.go` with package declaration
3. Create placeholder `sessions.go` with package declaration
4. Create placeholder `styles.go` with package declaration
5. Create placeholder `hooks/notify.sh`
6. Create placeholder `install.sh`

## Test Plan

- **Objective**: Verify file structure matches PRD specification
- **Success Criteria**: All files exist in correct locations

## Verification

- [ ] `hooks/` directory exists
- [ ] `main.go` exists
- [ ] `model.go` exists
- [ ] `sessions.go` exists
- [ ] `styles.go` exists
- [ ] `hooks/notify.sh` exists
- [ ] `install.sh` exists

## Files Modified

- `main.go` (created)
- `sessions.go` (created)
- `styles.go` (created)
- `hooks/notify.sh` (created)
- `install.sh` (created)
