# [6-1] Create directory setup

[Back to task list](./tasks.md)

## Description

Implement the portion of install.sh that creates the necessary directories.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-02-05 00:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |

## Requirements

1. Create `~/.claude-sessions/` directory
2. Create `~/.claude-sessions/hooks/` directory
3. Handle case where directories already exist (idempotent)
4. Print status messages

## Implementation Plan

Start `install.sh`:
```bash
#!/bin/bash
set -e

echo "Installing claude-sessions..."

# Create directories
SESSIONS_DIR="$HOME/.claude-sessions"
HOOKS_DIR="$SESSIONS_DIR/hooks"

mkdir -p "$SESSIONS_DIR"
echo "✓ Created $SESSIONS_DIR"

mkdir -p "$HOOKS_DIR"
echo "✓ Created $HOOKS_DIR"
```

## Test Plan

- **Objective**: Verify directories are created
- **Test Scenarios**:
  1. Fresh install - directories created
  2. Repeat run - no errors, idempotent
- **Success Criteria**: Directories exist after running

## Verification

- [ ] ~/.claude-sessions/ created
- [ ] ~/.claude-sessions/hooks/ created
- [ ] No errors on repeat run
- [ ] Status messages printed

## Files Modified

- `install.sh`
