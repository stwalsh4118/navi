# [2-2] Document hook configuration

[Back to task list](./tasks.md)

## Description

Create the hook configuration JSON that needs to be added to Claude Code's settings.json, documenting which hooks trigger which status updates.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-02-05 00:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2026-02-05 02:01:32 | Status Change | Proposed | InProgress | Started implementation | AI_Agent |
| 2026-02-05 02:01:52 | Status Change | InProgress | Review | Implementation complete, ready for review | AI_Agent |
| 2026-02-05 02:22:11 | Status Change | Review | Done | Task completed and verified | AI_Agent |

## Requirements

1. Document hook configuration for all relevant Claude Code events
2. Map events to status values:
   - Notification → `waiting`
   - Stop → `done`
   - PreToolUse → `permission`
   - SubagentStop → `working`

## Implementation Plan

Create `hooks/config.json` with the hook configuration:
```json
{
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [{ "type": "command", "command": "~/.claude-sessions/hooks/notify.sh waiting" }]
      }
    ],
    "Stop": [
      {
        "hooks": [{ "type": "command", "command": "~/.claude-sessions/hooks/notify.sh done" }]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [{ "type": "command", "command": "~/.claude-sessions/hooks/notify.sh permission" }]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [{ "type": "command", "command": "~/.claude-sessions/hooks/notify.sh working" }]
      }
    ]
  }
}
```

## Test Plan

- **Objective**: Verify configuration is valid JSON
- **Success Criteria**: JSON parses without errors

## Verification

- [x] Configuration documented for Notification hook
- [x] Configuration documented for Stop hook
- [x] Configuration documented for PreToolUse hook
- [x] Configuration documented for SubagentStop hook
- [x] JSON is valid and parseable

## Files Modified

- `hooks/config.json` (created)
